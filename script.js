document.addEventListener('DOMContentLoaded', function() { const csvData = `Header 1,Header 2,Header 3\nData 1,Data 2,Data 3\nData 4,Data 5,Data 6`; const csvTable = document.getElementById('csv-table'); const rows = csvData.split('\n'); rows.forEach((row, index) => { const tr = document.createElement('tr'); const cells = row.split(','); cells.forEach(cell => { const td = document.createElement(index === 0 ? 'th' : 'td'); td.textContent = cell; tr.appendChild(td); }); csvTable.appendChild(tr); }); const makeDraggable = (element) => { element.addEventListener('dragstart', (e) => { e.dataTransfer.setData('text/plain', null); }); }; makeDraggable(document.getElementById('draggable-card1')); makeDraggable(document.getElementById('draggable-card2')); });